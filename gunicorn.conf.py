bind = "0.0.0.0:10000"  # Use port 10000 as often required by Render
workers = 4  # Adjust based on your needs and server capacity
timeout = 120  # Increased timeout for potentially long-running requests
